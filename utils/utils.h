#ifndef UTILS_H
#define UTILS_H

#include "../types/entities.h"

void genProducts(Product* products, Relation* relations, int *quantidade, int lenCategories, int maxProdutos);
void find(Product* products, int quantity);


#endif // UTILS_H